# M1 exit gate — UI shell

## Automated

```bash
npm run lint && npm run typecheck && npm run build
```

## Manual

1. `/` — landing hero, Phaser mascot canvas, CTA to lobby.
2. `/lobby` — game grid, wallet HUD in header.
3. `/settings` — toggles render; reduced motion persists on reload.
4. No console errors during navigation.

## Tag

```bash
git tag -a m1 -m "M1: 8bitcn shell, Phaser mascot, lobby"
```
